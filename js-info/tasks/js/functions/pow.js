// Напишите функцию pow(x,n), которая возводит x в степень n и возвращает результат
function pow(a, b) {
  if ((a, b > 1)) {
    return console.log(Math.pow(a, b));
  } else {
    console.log('Какое-то число не соответствует!');
  }
}

pow(2, 5);
pow(3, 3);
pow(2, 2);
